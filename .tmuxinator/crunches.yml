name: crunches
root: ~/

attach: false
post: tmux -CC attach-session -t crunches

windows:
  - crunches:
      layout: main-vertical
      panes:
        - ssht crunch1 <%= @args.join(' ') %>
        - ssht crunch2 <%= @args.join(' ') %>
        - ssht crunch3 <%= @args.join(' ') %>
