name: <%= @args.size > 0 ? @args[0] : 'dcrunches' %>
root: ~/

attach: false
post: tmux -CC attach-session -t <%= @args.size > 0 ? @args[0] : 'dcrunches' %>

windows:
  - dcrunches:
      layout: tiled
      panes:
        - ssht crunch1 <%= @args.size > 0 ? @args.join(' ') : 'dcrunches' %>
        - ssht crunch2 <%= @args.size > 0 ? @args.join(' ') : 'dcrunches' %>
        - ssht crunch3 <%= @args.size > 0 ? @args.join(' ') : 'dcrunches' %>
        - ssht datastore <%= @args.size > 0 ? @args.join(' ') : 'dcrunches' %>
