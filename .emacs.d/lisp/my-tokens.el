;;; my-tokens.el --- Tokens used for authentication to services
;;; Commentary:
;;
;;; Code:

;; ignore locally by doing:
;; git -C ~/.emacs.d/lisp update-index --skip-worktree my-tokens.el

(provide 'my-tokens)
;;; my-tokens.el ends here
