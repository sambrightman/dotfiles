;;; my-work.el --- Work-specific functions
;;; Commentary:
;;
;;; Code:

;; copy, symlink and ignore locally (but commit in work repo) by doing:
;; git -C ~/.emacs.d/lisp update-index --assume-unchanged my-work.el

(provide 'my-work)
;;; my-work.el ends here
